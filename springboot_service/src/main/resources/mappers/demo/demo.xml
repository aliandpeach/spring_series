<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demoDAO">

    <select id="demoQuery" parameterType="int" resultType="Map">
        SELECT * FROM  t_s_role
    </select>
</mapper>