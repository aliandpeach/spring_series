<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yk.demo.dao.IRoleDAO">

    <select id="queryRoles" resultType="java.util.Map">
        SELECT * FROM  t_s_role;
    </select>
</mapper>